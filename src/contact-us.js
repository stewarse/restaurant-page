export default function contact() {

    const container = document.createElement('main')
    const title = document.createElement('h1')
    title.textContent = "Contact this Dick!"

    container.appendChild(title)

    return container
}